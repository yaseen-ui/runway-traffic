{
  "name": "flight-runway-scheduler",
  "version": "2.0.0",
  "description": "Flight runway scheduling system with pluggable scheduling strategies",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/server.js",
    "dev": "tsc && node dist/server.js",
    "clean": "rm -rf dist"
  },
  "keywords": ["flight", "scheduler", "runway", "typescript"],
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0"
  }
}
